Codigo de la clase 3.1, pero cambia el codigo porque en lugar de inicializar las variables una a una, 
se utiliza el binding.	
	-en Gradle.build:
		ViewBinding{
       			 enable = true } // la caracteristica viw binding se activa
	-uso de binding. (ActivityMainBinding)
	-Segunda pantalla y paso de una pantalla a otra
	- Intent para insertar acciones (enviar mensajes, abrir internet, cambiar de pantalla, etc.)